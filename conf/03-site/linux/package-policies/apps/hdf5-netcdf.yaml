packages:
  hdf5:
    prefer:
      - "@1.14:"
      - +cxx
      - +fortran
      - +hl
      - +map
      - +mpi
      - +shared
      - +subfiling
      - +szip
      - +threadsafe
      - +tools
  netcdf-c:
    prefer:
      - "@4.9:"
      - +blosc
      - +byterange
      - +fsync
      - +logging
      - +mpi
      - +nczarr_zip
      - +optimize
      - +parallel-netcdf
      - +pic
      - +shared
      - +szip
      - +zstd
  parallel-netcdf:
    prefer:
      - "@1.14:"
      - +burstbuffer
      - +cxx
      - +fortran
      - +pic
      - +shared
