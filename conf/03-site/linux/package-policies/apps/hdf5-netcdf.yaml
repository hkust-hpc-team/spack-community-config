packages:
  hdf5:
    prefer:
      - "@1.14:"
      - +cxx
      - +fortran
      - +hl
      - +map
      - +mpi
      - +shared
      - +subfiling
      - +threadsafe
      - +tools
    require:
      - +szip
  netcdf-c:
    prefer:
      - "@4.9:"
      - +blosc
      - +byterange
      - +fsync
      - +logging
      - +mpi
      - +nczarr_zip
      - +optimize
      - +pic
      - +shared
      - +szip
      - +zstd
    require:
      - +parallel-netcdf
  parallel-netcdf:
    prefer:
      - "@1.14:"
      - +burstbuffer
      - +cxx
      - +fortran
      - +pic
      - +shared
  parallelio:
    prefer:
      - "@2.6:"
      - +fortran
      - +mpi
      - +pnetcdf
      - +shared
      - +ncint
