packages:
  intel-oneapi-mpi:
    conflict:
      - "%gcc@11.5.0.spack"
    require:
      - ~external-libfabric
      - one_of:
          - "target=x86_64_v4 +generic-names %gcc@:"
          - "target=zen3 %aocc@:"
          - "%oneapi@:"
  libfabric:
    conflict:
      - "%gcc@11.5.0.spack"
    require:
      - +uring
      - fabrics=mlx,shm,sockets,tcp,ucx,udp,verbs
  openmpi:
    conflict:
      - "%gcc@11.5.0.spack"
    require:
      - +atomics
      - ~internal-hwloc
      - ~internal-libevent
      - ~internal-pmix
      - ~lustre
      - ~memchecker
      - ~openshmem
      - +romio
      - +rsh
      - +static
      - fabrics=cma,ucx
      - schedulers=slurm
      - +wrapper-rpath
      - spec: +legacylaunchers
        when: "@:4 schedulers=slurm"
      - one_of:
          - "target=zen4 %aocc@:"
          - "target=x86_64_v4 %nvhpc@:"
          - "target=x86_64_v4 %oneapi@:"
      - spec: ~cuda
        when: "%aocc@:"
      - spec: +cuda
        when: "%nvhpc@:"
      - spec: ~cuda
        when: "%oneapi@:"
  ucx:
    conflict:
      - "%nvhpc"
    require:
      - +cma
      - +dc
      - +dm
      - ~gdrcopy
      - ~ib_hw_tm
      - ~java
      - +knem
      - +mlx5_dv
      - +openmp
      - +optimizations
      - +pic
      - +rc
      - +rdmacm
      - ~rocm
      - +thread_multiple
      - +ucg
      - +ud
      - +verbs
      - ~xpmem
      - libs=shared,static
      - opt=3
      - simd=auto
      - one_of:
          - "target=zen4 %aocc@:"
          - "target=x86_64_v4 %oneapi@:"
          - "target=x86_64_v4 %gcc@11.5.0.spack"
      - spec: ~cuda
        when: "%aocc@:"
      - spec: +cuda
        when: "%gcc@11.5.0.spack"
      - spec: ~cuda
        when: "%oneapi@:"
