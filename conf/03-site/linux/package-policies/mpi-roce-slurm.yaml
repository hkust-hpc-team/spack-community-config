packages:
  intel-oneapi-mpi:
    conflict:
      - "%gcc@11.5.0.spack"
    require:
      - one_of:
          - "target=x86_64_v4 +generic-names %gcc@:"
          - "target=zen3 %aocc@:"
          - "%oneapi@:"
          - "%intel@:"
